name: Green Contribution

on:
  schedule:
    # Chạy mỗi ngày lúc 8:00 AM UTC (3:00 PM Vietnam)
    - cron: '0 8 * * *'
  workflow_dispatch: # Cho phép chạy thủ công

jobs:
  contribute:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Git
        run: |
          git config --global user.email "lieukienan2004@gmail.com"
          git config --global user.name "Lieu Kien An"
          
      - name: Make Contribution
        run: |
          # Tạo/cập nhật file với timestamp
          echo "Last updated: $(date)" > .github/LAST_UPDATED.md
          echo "" >> .github/LAST_UPDATED.md
          echo "🟢 Auto contribution by GitHub Actions" >> .github/LAST_UPDATED.md
          echo "📅 $(date '+%Y-%m-%d %H:%M:%S UTC')" >> .github/LAST_UPDATED.md
          
      - name: Commit and Push
        run: |
          git add .
          git commit -m "🟢 Daily contribution $(date '+%Y-%m-%d')" || echo "No changes"
          git push
